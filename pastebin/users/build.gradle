version = "0.1"
group = "users"

jib.to.image = "gitaroktato/pastebin/users"

micronaut {
    processing {
        annotations("users.*")
    }
}

application {
    mainClass.set("users.Application")
}
